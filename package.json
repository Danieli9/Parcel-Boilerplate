{
  "name": "my-project",
  "source": "src/index.html",
  "browserslist": "> 0.2%, last 2 versions, not dead",
  "scripts": {
    "dev": "parcel ./src/*.html",
    "build": "parcel build --public-url ./ ./src/*.html --dist-dir build"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.7.0",
    "cssnano": "^5.1.12",
    "parcel": "^2.7.0",
    "postcss": "^8.2.15"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.0"
  }
}
